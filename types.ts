
export interface ServiceItem {
  id: string;
  title: string;
  code: string;
  description: string;
  highlight?: boolean;
}

export interface SectionContent {
  label: string;
  description: string;
  linkText?: string;
}
