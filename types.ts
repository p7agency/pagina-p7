
export interface ServiceItem {
  id: string;
  title: string;
  code: string;
  description: string;
}

export interface SectionContent {
  label: string;
  description: string;
  linkText?: string;
}
